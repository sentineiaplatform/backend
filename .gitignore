# Maven
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
release.properties
.flattened-pom.xml
dependency-reduced-pom.xml

# Maven Wrapper (opcional: versionar `mvnw`, `.mvn/wrapper/maven-wrapper.properties`)
.mvn/wrapper/maven-wrapper.jar

# IDE
.idea/
*.iml
*.ipr
*.iws
.project
.classpath
.settings/
.factorypath
.vscode/
*.swp
*~
.sts4-cache/
.apt_generated/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Variáveis locais / segredos
.env
.env.*
!.env.example
secrets.properties

# Spring Boot — overrides com passwords / URLs (não versionar)
application-local.properties
application-*-local.properties
application-local.yml
application-*-local.yml
application-dev.properties

# H2 em arquivo (se usar JDBC file em dev)
*.mv.db
*.trace.db

# JVM crash dumps
hs_err_pid*
replay_pid*

# Relatórios de cobertura (se usar JaCoCo / Surefire fora de target)
jacoco.exec
*.exec
